features
* higher order functions
* adts
* io
* strict probaly becasue lazyness is alittle bit harder to make
* if then else syntax sugar for case syntax
* module system


fun name(a,b,c) {body};

val name = {

}

pub data Name param param = Abc type type type | DFG type type;
-- type or type param

pub fun helloWorld() {
    name = input;
    print $ "hello " ++ name;
}
    
# operator precedence
# fun _*_():12 { -- the 12 is the precedence

# } probaly wont have operator precedence

fun test(x,y) 2 * x + y;

import modulename.thing.thing


